import React from 'react';
import { Button, Form } from 'react-bootstrap';

const Login = () => {
    return (
        <div>
            <Form>id : </Form>
            <Form>password : </Form>
            <Button>Login</Button>
        </div>
    );
};

export default Login;